# Optional. Enables live relative-demand and concentration evidence for searched artists.
# This key is read only by the server route and is never sent to the browser.
LASTFM_API_KEY=

# Optional. Adds cumulative view counts for conservatively matched official music videos,
# official audio, and artist-topic uploads. It does not infer annual streams or revenue.
YOUTUBE_API_KEY=
